[project]
name = "pa-milky-simplified"
version = "0.1.0"
description = "Ideal-world Legacy 25K PA book simulator, built one rule at a time"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
