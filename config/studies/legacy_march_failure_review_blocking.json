{
  "execution": "blocking",
  "pipeline_spec": "config/studies/legacy_pipeline_capacity_blocking.json",
  "note": "Blocked-copying counterpart of the March 2026 failure review (scripts/review_march_failure.py, which has no non-blocking spec file). The pipeline spec fixes the execution tree; the two minimum winners come from the blocked pipeline study, and the frozen cases (daily maximum at $5,700, $6,700, $6,800 and daily minimum at $6,700, $6,800 on the reserve-frontier pipelines) are unchanged. The recovery stress reads the same spec."
}
